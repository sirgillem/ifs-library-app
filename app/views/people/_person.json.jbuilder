json.extract! person, :id, :pre_titles, :pre_names, :key_name_prefix, :key_name, :post_names, :key_name_suffix, :qualifications, :post_titles, :created_at, :updated_at
json.url person_url(person, format: :json)
