<% provide :title, @song_template %>
<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @song_template.name %>
</p>

<%= render partial: 'song_parts/member_list', locals: { resource: @song_template } %>

<%= link_to 'Edit', edit_song_template_path(@song_template) %> |
<%= link_to 'Back', song_templates_path %>
