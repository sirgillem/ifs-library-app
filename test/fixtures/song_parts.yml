opus_one_tenor1:
  song: opus_one
  name: Tenor 1
  scanned: true
  notes: No solo

<% parts = ['alto1','alto2','tenor1','tenor2','bari'] %>
<% parts.each do |part| %>
  templ_<%= part %>:
    name: <%= part %>
    template: bigband
<% end %>
